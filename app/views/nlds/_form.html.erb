<%= form_for(@nld) do |f| %>
  <% if @nld.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@nld.errors.count, "error") %> prohibited this nld from being saved:</h2>

      <ul>
      <% @nld.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :NLD %><br>
    <%= f.text_field :NLD %>
  </div>
  <div class="field">
    <%= f.label :Name %><br>
    <%= f.text_field :Name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
